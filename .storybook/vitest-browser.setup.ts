// Setup for the non-Storybook browser project (visual regression). Loads the
// same Tailwind + @qovira/theme pipeline the app/Storybook use, so rendered
// components are styled in the browser test environment.
import "../src/app.css";
