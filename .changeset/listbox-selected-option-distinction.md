---
"@qovira/ui": patch
---

Selected options are now clearly distinguished in the open Select and Combobox listboxes: a selected row carries an accent-tinted background and medium weight in addition to the existing checkmark, in both single and multiple modes. Previously the only selected cue was a faint checkmark, which was hard to scan — especially in a multi-select list.
