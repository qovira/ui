---
"@qovira/ui": patch
---

The Combobox now opens its options list when the search input is clicked, not only when the trigger icon is clicked. Typeahead filtering, the trigger toggle, and keyboard behavior are unchanged; the list is intentionally not opened on focus so that tabbing through a form doesn't auto-open it.
